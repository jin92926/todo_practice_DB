{
  "name": "json-server-exam",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server-auth --watch db.json --port 4000"
  },
  "devDependencies": {
    "json-server": "^0.17.2",
    "json-server-auth": "^2.1.0"
  },
  "dependencies": {
    "lowdb": "^5.1.0"
  }
}
